<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingBottom="24dp"
            android:text="Manage contacts"
            android:textColor="@color/oneplus_text_primary"
            android:textSize="24sp"
            android:textStyle="bold" />

        <com.google.android.material.switchmaterial.SwitchMaterial
            android:id="@+id/switch_profile_pic"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingVertical="8dp"
            android:text="Show contacts profile picture"
            android:textColor="@color/oneplus_text_primary" />

        <com.google.android.material.switchmaterial.SwitchMaterial
            android:id="@+id/switch_only_numbers"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingVertical="8dp"
            android:text="Only show contacts with numbers"
            android:textColor="@color/oneplus_text_primary" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="16dp"
            android:paddingBottom="8dp"
            android:text="Contacts to display"
            android:textColor="@color/oneplus_text_secondary"
            android:textSize="12sp"
            android:textAllCaps="true" />

        <!-- ... Sort By ... -->
        <include
            android:id="@+id/row_sort_by"
            layout="@layout/item_settings_row" />

        <include
            android:id="@+id/row_import_export"
            layout="@layout/item_settings_row" />

        <include
            android:id="@+id/row_sim_contacts"
            layout="@layout/item_settings_row" />

        <include
            android:id="@+id/row_merge_contacts"
            layout="@layout/item_settings_row" />

    </LinearLayout>

</androidx.core.widget.NestedScrollView>
